# To run the project:

```
# Create Pipfile
pipenv --three

# Open Pipfile
pipenv shell

# Install package
pipenv install <package>

# Run Python Project
pipenv run python3 project/tokens/token_category.py
```